int n1 = 0, n2 = 0;
    // if(root->left)n1 = postorder(root->left);
    // if(root->right)n2 = postorder(root->right);
    // if(n1 != n2){flag2 = 0;return max(n1,n2);}
    // else return root->co == BLK ? n1 + 1 : n1;